# Baseline: the engine as delivered

Measurements taken against the engine exactly as vendored from the design
artifact, before any correctness fix was applied. Kept so the cost of each fix
is visible rather than asserted.

The engine at that point is commit `101a4e7`; `docs/architecture-review.md`
cites line numbers in that revision. Do not regenerate these — to rebuild a
baseline, check out that commit and write into a new directory.
