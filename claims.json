{
  "_comment": [
    "Every statement this project makes about itself, with the state the recorded",
    "measurements are expected to be in. `verify` checks this file against results/.",
    "",
    "expect: 'holds'  - the statement is true and must stay true.",
    "        'fails'  - a known, accepted limitation. It is written down so that it",
    "                   cannot be quietly forgotten, and so that fixing it is a",
    "                   deliberate act that updates this file.",
    "        'not_exercised' - the profile cannot reach the condition.",
    "",
    "A finding that flips in EITHER direction fails verification. A claim that",
    "expected a real result but got 'not_exercised' also fails: an untested hazard",
    "must never read as a green build."
  ],
  "findings": [
    {
      "experiment": "f5-latency",
      "id": "F5.1",
      "expect": "fails",
      "because": "Append latency is long-tailed: inline merge_key holds both the shard lock and the appender lock while it decompresses, concatenates, recompresses and rewrites a key's whole run. The design document reports only throughput means, which cannot show this."
    },
    {
      "experiment": "f5-latency",
      "id": "F5.2",
      "expect": "holds",
      "because": "No single append should stall for more than 50ms. This currently holds at ci profile and is the guard that would catch inline merging getting worse."
    },
    {
      "experiment": "f6-threads",
      "id": "F6.1",
      "expect": "fails",
      "because": "Write throughput does not scale with writer threads. seal_shard takes the single appender mutex inside its per-extent loop and flush_builder takes it twice consecutively, so the write path convoys. Measured at 19% efficiency and negative absolute scaling at 4 threads. Every db_bench comparison in the design document is single-threaded, which is the configuration that hides this."
    },
    {
      "experiment": "f2-open",
      "id": "F2.1",
      "expect": "fails",
      "because": "Reader::build materialises the entire key index on the heap -- one Vec<u8> per key plus a 2N-slot hash table -- so open is linear in key count and paid per process, shared with nobody. This is Bitcask's design, not the LMDB many-reader-process model the architecture claims."
    },
    {
      "experiment": "f2-open",
      "id": "F2.2",
      "expect": "fails",
      "because": "Open cost is measured super-linear (20x the keys costs 30x the open), so it is not merely un-amortised, it degrades. Fixing this means a mmap-able, binary-searchable index shared through the page cache."
    },
    {
      "experiment": "f4-durability",
      "id": "F4.1",
      "expect": "fails",
      "because": "A 1,000-op durability window costs 25x throughput, because checkpoint() rewrites the whole key index rather than what changed."
    },
    {
      "experiment": "f4-durability",
      "id": "F4.2",
      "expect": "fails",
      "because": "There is no usable point on the durability curve: either lose everything buffered since the last checkpoint (512MB by default), or pay an O(nkeys) index rewrite per commit. An incremental checkpoint is the single largest piece of remaining work."
    },
    {
      "experiment": "f3-multiproc",
      "id": "F3.1",
      "expect": "holds",
      "because": "Every reader process must see a complete, self-consistent checkpoint state. This is the core safety property of the append-only design and must never regress."
    },
    {
      "experiment": "ext-kv",
      "id": "EXT.1",
      "expect": "holds",
      "because": "Supdb loads faster than LMDB, the architecture it is modelled on. This is the design's real and defensible win and must not regress."
    },
    {
      "experiment": "ext-kv",
      "id": "EXT.4",
      "expect": "fails",
      "because": "Measured through native bindings LMDB reads about 2.4x faster than Supdb. The design document reports the opposite ordering, but measured LMDB through a Java harness with an adapter it separately found to allocate per value and open a transaction per lookup. Removing the language boundary reverses the result."
    },
    {
      "experiment": "ext-kv",
      "id": "EXT.5",
      "expect": "fails",
      "because": "LMDB scans roughly 4.7x faster than Supdb natively."
    },
    {
      "experiment": "ext-kv",
      "id": "EXT.6",
      "expect": "holds",
      "because": "Supdb stores the same data in less space than LMDB. Compactness is a genuine strength of the design."
    },
    {
      "experiment": "ext-ycsb",
      "id": "EXT.3",
      "expect": "fails",
      "because": "On YCSB-A (50/50 read/write) Supdb runs 13.5x slower than on read-only YCSB-C. Store exposes no read method, so a read following a write needs a checkpoint plus a fresh Reader, both O(key count). No benchmark in the design document mixes reads and writes, which is why this never appeared."
    },
    {
      "experiment": "f1-outofcore",
      "id": "F1.1",
      "expect": "holds",
      "because": "A cold measurement must be able to prove it was cold. The design document confesses that its earlier cold-read numbers were served from pages the JVM had pinned across drop_caches, invalidating all of them."
    },
    {
      "experiment": "f1-outofcore",
      "id": "F1.2",
      "profile": "ci",
      "expect": "not_exercised",
      "because": "The ci profile builds 64MB on a 16GB machine, so the dataset never leaves the page cache. Recorded as not-exercised rather than passing, because an untested hazard must not read as a green build."
    },
    {
      "experiment": "f1-outofcore",
      "id": "F1.2",
      "profile": "full",
      "expect": "fails",
      "because": "Measured at 23.0GB against 15.7GB of RAM: 338,681 reads/s resident against 370 out-of-core, a 916x degradation. The engine reads through a read-only mmap with no madvise anywhere, so it has no readahead control, no asynchronous I/O and no influence over eviction -- the failure modes Crotty et al. (CIDR'22) enumerate."
    },
    {
      "experiment": "f1-outofcore",
      "id": "F1.4",
      "profile": "full",
      "expect": "fails",
      "because": "Out-of-core read latency is bimodal: p50 0.155ms but p99 9.5ms and max 81ms. Every cache miss is a synchronous page fault the engine cannot see coming or overlap."
    },
    {
      "experiment": "f1-outofcore",
      "id": "F1.3",
      "profile": "full",
      "expect": "holds",
      "because": "The precondition: the stored file must exceed the memory available to cache it. 23.0GB against 15.7GB, ratio 1.47."
    },
    {
      "experiment": "f7-index",
      "id": "F7.1",
      "expect": "fails",
      "because": "Reader memory is linear in key count. Reader::build allocates one Vec<u8> per key plus a 2N-slot hash table, all on the heap, all before the first read."
    },
    {
      "experiment": "f7-index",
      "id": "F7.2",
      "expect": "fails",
      "because": "About 131 bytes of resident index per key, to index a 100-byte value -- and paid again by every reader process, shared with nobody. In RUM terms this is the memory the design spends to buy read performance; the objection is that it was unmeasured and that the stated architecture is many reader processes sharing one mapping."
    },
    {
      "experiment": "f7-index",
      "id": "F7.3",
      "expect": "holds",
      "because": "Eight concurrent readers must still hold a 10M-key store on a 16GB machine. It holds with thin margin -- the ceiling is about 16M keys each -- so this is the guard that would catch the per-key cost growing."
    }
  ],
  "metrics": [
    {
      "experiment": "f5-latency",
      "path": "series.throughput.ops_per_s",
      "min": 100000,
      "because": "A floor, not a target. The per-call timer is inside the loop here, so this is well below headline throughput; it exists to catch a collapse."
    },
    {
      "experiment": "f6-threads",
      "path": "series.scaling.0.ops_per_s",
      "min": 200000,
      "because": "Single-threaded append throughput. The axis the design is built to win, so a regression here is the most serious kind."
    },
    {
      "experiment": "f3-multiproc",
      "path": "series.readers.read_errors",
      "max": 0,
      "because": "Zero read errors across reader processes. Any nonzero value means a reader observed space the writer reused underneath it."
    },
    {
      "experiment": "f3-multiproc",
      "path": "series.readers.torn_or_inconsistent",
      "max": 0,
      "because": "Zero torn or inconsistent states."
    },
    {
      "experiment": "ext-kv",
      "path": "series.engines.0.read_hit_rate",
      "min": 0.99,
      "because": "The comparison is only meaningful if Supdb actually returns the data. A collapse here would mean the reads being timed are misses."
    }
  ]
}